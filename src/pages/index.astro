---
export const prerender = true;
import WordTree from "../assets/wordtree.mp4";
import { About } from "../components/About";
import { Experience } from "../components/Experience/Experience";
import Headshot from "../components/Headshot.astro";
import { WhoAmI } from "../components/WhoAmI";
import Layout from "../layouts/Layout.astro";
import Theme from "../layouts/Theme.astro";
import PauseAnimationButton from "../components/PauseAnimationButton.astro";
import { LoaderCircle } from "lucide-react";
---

<script is:inline>
	function togglePause() {
		const main = document.querySelector("main");
		main.classList.toggle("**:paused");
		document.getElementById("pause-button").classList.toggle("hidden");
		document.getElementById("play-button").classList.toggle("hidden");
		const video = document.querySelector("video");
		video.paused ? video.play() : video.pause();
	}
</script>

<Theme />
<Layout>
	<WhoAmI>
		<Headshot slot="headshot" />
	</WhoAmI>
	<About />
	<Experience />
	<div
		class="flex w-full max-w-[1200px] flex-col-reverse justify-between lg:flex-row"
	>
		<div
			class="flex min-w-[360px] flex-col gap-6 p-8 lg:max-w-[1280px] lg:p-16"
		>
			<a
				href="https://www.wordtree.app"
				target="_blank"
				class="outline-accent bg-accent relative block w-full -rotate-3 rounded-lg pr-8 outline-6 transition-transform hover:scale-105 hover:rotate-3"
			>
				<video
					autoplay
					loop
					muted
					playsinline
					class="bg-background aspect-video rounded-lg"
				>
					<source
						src="/video/wordtree-demo.mp4"
						type="video/mp4"
						class="aspect-video min-h-full min-w-full"
					/>
				</video>
				<div
					class="bg-secondary absolute top-[calc(50%-12px)] right-0 h-6 w-6 rounded-full outline outline-gray-600"
				>
				</div>
			</a>
		</div>
		<div class="flex flex-col gap-6 p-8 lg:max-w-sm">
			<h2 class="text-2xl font-bold">WordTree</h2>
			<p class="text-justify">
				<a
					href="https://www.wordtree.app"
					class="underline"
					target="_blank">WordTree</a
				> is a cross-platform app built with React Native and Expo. It challenges
				players to think strategically and plan ahead while testing their
				vocabulary.
			</p>
			<p class="text-justify">
				What started as a fun side project created in 2023 while
				experimenting with developing cross-platform applications,
				WordTree now has over 1,000 monthly active users. Give it a try!
			</p>
		</div>
		<PauseAnimationButton />
	</div>
</Layout>
